apiVersion: apps/v1
kind: Deployment
metadata:
  name: user14
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user14
  template:
    metadata:
      labels:
        app: user14
    spec:
      containers:
        - name: user14-server
          image: ghcr.io/andapattanachoo/devops-1
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: user14
  labels:
    app: user14
spec:
  selector:
    app: user14
  ports:
    - name: http
      protocol: TCP
      port: 9999
      targetPort: 80
  type: ClusterIP